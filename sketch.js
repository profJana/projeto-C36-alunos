var canvas, backgroundImage;

var questions;

var question, contestant, quiz;


function setup(){
  canvas = createCanvas(850,400);

  //como referencia o banco de dados corretamente na variável:
  //database = firebase.database();
  //database = database();
  //database = firebase.database;
  //firebase.database();


  //como cria corretamente um novo Quiz
  //quiz = Quiz();
  //quiz = new Quiz;
  //new Quiz();
  //quiz = new Quiz();
  
  quiz.start();
}


function draw(){
  background("pink");

}
